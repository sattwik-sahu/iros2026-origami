# Hugging Face Hub push configuration.
#
# Set `push: true` to upload the trained model to the hub at the end of training.
# Authenticate first with:  huggingface-cli login
push: true
repo_id: sattwik21/sharpa-north-origami
private: false
