# Optimizer and learning-rate schedule configuration.
lr: 1.0e-4
betas: [0.9, 0.95]
weight_decay: 1.0e-4
grad_clip: 1.0

scheduler: cosine_with_warmup
warmup_steps: 1000
total_steps: 20000
lr_min: 1.0e-6
